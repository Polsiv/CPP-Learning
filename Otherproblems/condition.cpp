#include <iostream>

int main() {

  using namespace std;

  int i, j;

  double a, b, completo;

  a=12;
  i=12;
  j=30;
  b=2.0;
  completo=0.0;

  cout<<"a > b "<<(a>b)<<endl;
 
  
  cout<<"(( i==j) || (a<b) || completo):"<<((i==j) || (a<b) || completo)<<endl;

  cout<<"((a/b>5)&&(i<=b20)): " <<((a/b>5)&&(i<=20))<<endl;



  
  std::cout << "Hello World!\n";
}